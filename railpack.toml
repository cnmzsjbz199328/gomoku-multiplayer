[build]
install = "npm ci"
preBuild = "cp -r shared backend/shared && echo '✓ Copied shared to backend'"
build = "npm run build --workspace=backend && npm run build --workspace=frontend"

[start]
cmd = "npm run start --workspace=backend"

[environments]
NODE_ENV = "production"
